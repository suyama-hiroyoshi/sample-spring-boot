# 使用しているCircleCIプラットフォームのバージョン。2.1が最新。
version: 2.1

# 使用するOrbsを指定する。指定する値はOrbsの説明を見る。
orbs: 
  maven: circleci/maven@1.3.0

# テストを実行するワークフロー
workflows:
  maven_test:
    triggers:
      - schedule:
          cron: "0,10,20,30,40,50 * * * *" # UTCの日時
          filters:
            branches:
              only:
                - master
    jobs:
      - maven/test:
          # completeフォルダを指定する
          app_src_directory: complete
          command: '-X verify'